name: Sample build workflow
on: push

jobs:
    build-deploy:
        runs-on: windows-latest
        steps:
        - uses: actions/checkout@v1
        - name: Build
          shell: pwsh
          run: |
                  .\build.ps1
